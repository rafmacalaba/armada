---
description: Report armada repository state — manifest, voyages, ledgers, and what evidence the next gate requires
---
Check the on-disk armada state. Run the `armada_status` tool (or `armada status --json` via bash) and inspect `armada/state/` if present. Report where each phase stands, which ledgers are pending, and what evidence the next gate requires.
