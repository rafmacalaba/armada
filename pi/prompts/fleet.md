---
description: Show active armada voyages (lanes, phases, gates) and suggest the next action per lane
---
Run the `armada_fleet` tool (or `armada fleet` via bash if unavailable). For each lane, report: phase, gate status, staleness, and cost. Then suggest the next action per lane — continue the current phase, loop defects back to the developer, or finish per the `armada-voyage-finish` skill.
