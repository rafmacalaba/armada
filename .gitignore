node_modules/
*.log
.DS_Store
dist/
build/
.armada-cache/
# Local opencode plugin list side-effect; not a real config
/.opencode/
# Generated outputs used in tests / validation
/tmp-test/
*.tgz
# Plain-copy sandboxes (git worktree paths are git-managed, no entry needed)
/sandbox/
# Sandbox worktrees are git-managed; the directories they occupy are not repo files
/.slim/
# Planning artifacts (specs/plans) stay local, not in the repo; the repo keeps TODO.md
/docs/superpowers/

# armada runtime state (contracts, state, active) — not source
/armada/
/opencode.json

# Validation evidence-target (sandbox facsimile of ~/WBG/data-ai-chatbot, gitignored build artifact)
/evidence-target/

# armada:process
/DEFECTS.md
/ADVERSARIAL_REVIEW.md
/AUDIT.md
# armada:process:end
