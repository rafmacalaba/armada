# {project_name}

> Instructions for AI agents and humans working in this repository.

## Repository conventions

- Paper source in `main.tex`; bibliography in `references.bib`.
- One idea per section; cite every claim; no unreferenced assertions.
- Keep `\cite` keys matching `references.bib` exactly.
- No emojis in prose.

## Workflow

1. Read the task/contract before touching code.
2. Draft in `main.tex`, add citations to `references.bib`.
3. Run `make` — must produce `main.pdf` without errors.
4. Verify no missing citations (`make` output shows undefined refs).

## Quality bar

- Every claim has a citation.
- The PDF builds cleanly from a fresh clone (`make clean && make`).
- Generated by armada — do not edit this header.
