# {project_name}

> Instructions for AI agents and humans working in this repository.

## Repository conventions

- Python, type hints on all public functions.
- Model definition in `src/model.py`; the training loop in `src/train.py`.
- Deterministic seeds in training code (reproducible runs).
- No emojis in code, comments, print statements or logging.

## Workflow

1. Read the task/contract before touching code.
2. Write a failing test first (`tests/`), then implement.
3. Run `uv run pytest` — keep it green.
4. Run `uv run ruff check src/` before claiming done.

## Quality bar

- Every public function in `src/` has a unit test.
- No `print()` left in library code paths (use logging).
- Generated by armada — do not edit this header.
