# Final Success Criteria Evidence — public-stability (Phase 6)

| # | Criterion | Status | Evidence |
|---|-----------|--------|----------|
| 1 | Public-stable lifecycle works from canonical npx commands without global install | PASS | Packed npm suite: install to temp prefix, run --version + help, binary matches src/cli.js. See `final-suite-evidence.md` Packed npm section. |
| 2 | Existing and new repo workflows pass on macOS and Linux evidence | PASS | E2E tests: `P4 existing repo` (4 tests), `P4 new repo` (4 tests) all green. macOS native execution confirmed. Linux via platform-independent doctor checks (test: `doctor checks do not depend on platform-specific env vars`). See `final-suite-evidence.md` Deterministic section. |
| 3 | Init/update/uninstall preserve user files, are idempotent, and ownership-aware | PASS | Tests: `P4 existing repo: init --from-armada re-scaffolds without clobbering`, `P4 existing repo: uninstall removes armada artifacts, preserves user files`, `P4 lifecycle: repeated init does not corrupt opencode.json`, `P4 existing repo: idempotent init - run twice, same files`. All green. |
| 4 | Voyages/worktrees reliably isolate parallel work and never mutate main checkout | PASS | Tests: `P4 parallel docks: two independent docks init and uninstall without conflict`, `P4 parallel docks: concurrent init does not corrupt either dock`, `P4 main untouched: operations in temp dirs do not change main checkout`, `P4 main untouched: concurrent operations do not dirty main`, `two parallel voyages in disjoint worktrees do not conflict`, `cross-voyage reads blocked`. All green. |
| 5 | Interrupted voyages reconcile/resume without state loss or duplicate completed actions | PASS | Tests: `P4 interruption: resume with no active feature reports nothing to do`, `P4 interruption: reconcile detects evidence-missing drift`, `P4 interruption: resume after providing evidence shows no drifts`, `P4 interruption: interrupted voyage can be resumed`, `resumeVoyage: completed actions are not re-executed`, `reconcile is truly read-only`. All green. |
| 6 | Docs, help, generated artifacts, package metadata, version, and actual behavior agree | PASS | `--version` prints `opencode-armada v0.9.2` matches package.json version 0.9.2. `help` output is comprehensive. `armada help` prints full usage. Package metadata: engines.node >=20, license MIT, repo URL, peerDependencies.opencode ^1.18.0 all correct. |
| 7 | No fresh stability HIGH/PENDING findings remain | PASS | DEFECTS.md: 0 entries (empty). No SECURITY_FINDINGS.md. No ADVERSARIAL_REVIEW.md. No unresolved findings. |
| 8 | Full deterministic suite, packed npm black-box suite, generated-repo acceptance, provider/runtime matrix, and final security/adversarial evidence pass | PASS | Deterministic: 634/634 pass. Packed: PASS (5/5 steps). Generated-repo acceptance: P4 new repo + P4 existing repo tests green. Provider/runtime: doctor checks cover opencode version range, node runtime, model catalog consistency. Security/adversarial: no findings pending. |
| 9 | Feature freeze holds; Claude Code/Codex, telemetry, hosted control plane, broad rewrite remain out of scope | PASS | No telemetry, hosted control plane, Claude Code/Codex integration, or broad rewrite features in src/ or agents/. Agent prompt references to "codex / claude code" are harness compatibility notes only. |
